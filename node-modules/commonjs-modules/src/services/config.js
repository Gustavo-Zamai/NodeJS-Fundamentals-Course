// exports const
const devArea = {
    version: '1.0.1',
    production: true,
};

// module.exports = devArea; if only one thing was export

const client = {
    device: 'web',
};

module.exports = {
    devArea,
    client,
};